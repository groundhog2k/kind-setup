app:
  ingress:
    enabled: true
    useDefaultIngressClass: true
    hosts:
      - "dash.k8s.local"
    issuer:
      name: local-ca
      scope: cluster
    tls:
      secretName: dash-k8s-local

api:
  scaling:
    replicas: 1
