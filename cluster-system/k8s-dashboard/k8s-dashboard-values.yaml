kong:
  enabled: false

app:
  ingress:
    enabled: false
    useDefaultIngressClass: true
    hosts:
      - "k8sdash"
    issuer:
      name: local-ca
      scope: cluster
    tls:
      secretName: k8sdash

